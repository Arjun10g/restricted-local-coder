{
  "name": "restricted-local-coder",
  "displayName": "Restricted Local Coder",
  "description": "Self-contained local coding assistant with a bundled llama.cpp runtime and no cloud inference.",
  "version": "0.4.2",
  "publisher": "restricted-local",
  "license": "MIT",
  "engines": {
    "vscode": "^1.95.0"
  },
  "categories": [
    "Other",
    "Programming Languages"
  ],
  "keywords": [
    "local ai",
    "offline",
    "coding assistant",
    "gguf",
    "llama.cpp",
    "qwen coder"
  ],
  "main": "./src/extension.js",
  "extensionKind": [
    "ui"
  ],
  "activationEvents": [
    "onView:localCoder.chat",
    "onCommand:localCoder.openChat",
    "onCommand:localCoder.start",
    "onCommand:localCoder.stop",
    "onCommand:localCoder.restart",
    "onCommand:localCoder.downloadModel",
    "onCommand:localCoder.importModel",
    "onCommand:localCoder.selectModel",
    "onCommand:localCoder.preflight",
    "onCommand:localCoder.askSelection",
    "onCommand:localCoder.explainSelection",
    "onCommand:localCoder.reviewSelection",
    "onCommand:localCoder.refactorSelection",
    "onCommand:localCoder.generateTests",
    "onCommand:localCoder.debugSelection",
    "onCommand:localCoder.insertLastResponse",
    "onCommand:localCoder.replaceSelectionWithLastResponse",
    "onCommand:localCoder.cancelGeneration",
    "onCommand:localCoder.showLogs",
    "onCommand:localCoder.editProjectMemory",
    "onCommand:localCoder.showAgentAudit"
  ],
  "capabilities": {
    "untrustedWorkspaces": {
      "supported": false,
      "description": "The extension reads workspace source files to construct local model context."
    }
  },
  "contributes": {
    "commands": [
      {
        "command": "localCoder.openChat",
        "title": "Local Coder: Open Chat",
        "icon": "$(comment-discussion)"
      },
      {
        "command": "localCoder.start",
        "title": "Local Coder: Start Local Runtime",
        "icon": "$(play)"
      },
      {
        "command": "localCoder.stop",
        "title": "Local Coder: Stop Local Runtime",
        "icon": "$(debug-stop)"
      },
      {
        "command": "localCoder.restart",
        "title": "Local Coder: Restart Local Runtime",
        "icon": "$(refresh)"
      },
      {
        "command": "localCoder.downloadModel",
        "title": "Local Coder: Download or Repair Model",
        "icon": "$(cloud-download)"
      },
      {
        "command": "localCoder.importModel",
        "title": "Local Coder: Import Existing GGUF Model",
        "icon": "$(folder-opened)"
      },
      {
        "command": "localCoder.selectModel",
        "title": "Local Coder: Select Model Profile",
        "icon": "$(symbol-parameter)"
      },
      {
        "command": "localCoder.preflight",
        "title": "Local Coder: Run Preflight",
        "icon": "$(checklist)"
      },
      {
        "command": "localCoder.askSelection",
        "title": "Local Coder: Ask About Selection",
        "icon": "$(comment)"
      },
      {
        "command": "localCoder.explainSelection",
        "title": "Local Coder: Explain Selection",
        "icon": "$(book)"
      },
      {
        "command": "localCoder.reviewSelection",
        "title": "Local Coder: Review Selection",
        "icon": "$(inspect)"
      },
      {
        "command": "localCoder.refactorSelection",
        "title": "Local Coder: Refactor Selection",
        "icon": "$(tools)"
      },
      {
        "command": "localCoder.generateTests",
        "title": "Local Coder: Generate Tests for Selection",
        "icon": "$(beaker)"
      },
      {
        "command": "localCoder.debugSelection",
        "title": "Local Coder: Debug Selection",
        "icon": "$(debug-alt)"
      },
      {
        "command": "localCoder.insertLastResponse",
        "title": "Local Coder: Insert Last Response at Cursor",
        "icon": "$(insert)"
      },
      {
        "command": "localCoder.replaceSelectionWithLastResponse",
        "title": "Local Coder: Replace Selection with Last Response",
        "icon": "$(replace)"
      },
      {
        "command": "localCoder.cancelGeneration",
        "title": "Local Coder: Cancel Generation",
        "icon": "$(circle-slash)"
      },
      {
        "command": "localCoder.showLogs",
        "title": "Local Coder: Show Logs",
        "icon": "$(output)"
      },
      {
        "command": "localCoder.editProjectMemory",
        "title": "Local Coder: Edit Project Memory",
        "icon": "$(note)"
      },
      {
        "command": "localCoder.showAgentAudit",
        "title": "Local Coder: Show Agent Audit Log",
        "icon": "$(list-flat)"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "localCoder",
          "title": "Local Coder",
          "icon": "media/localcoder.svg"
        }
      ]
    },
    "views": {
      "localCoder": [
        {
          "id": "localCoder.chat",
          "name": "Private Coding Chat",
          "type": "webview",
          "contextualTitle": "Local Coder"
        }
      ]
    },
    "menus": {
      "editor/context": [
        {
          "command": "localCoder.askSelection",
          "when": "editorHasSelection",
          "group": "localCoder@1"
        },
        {
          "command": "localCoder.explainSelection",
          "when": "editorHasSelection",
          "group": "localCoder@2"
        },
        {
          "command": "localCoder.reviewSelection",
          "when": "editorHasSelection",
          "group": "localCoder@3"
        },
        {
          "command": "localCoder.refactorSelection",
          "when": "editorHasSelection",
          "group": "localCoder@4"
        },
        {
          "command": "localCoder.generateTests",
          "when": "editorHasSelection",
          "group": "localCoder@5"
        }
      ],
      "view/title": [
        {
          "command": "localCoder.start",
          "when": "view == localCoder.chat",
          "group": "navigation@1"
        },
        {
          "command": "localCoder.stop",
          "when": "view == localCoder.chat",
          "group": "navigation@2"
        },
        {
          "command": "localCoder.selectModel",
          "when": "view == localCoder.chat",
          "group": "navigation@3"
        }
      ]
    },
    "configuration": {
      "title": "Restricted Local Coder",
      "properties": {
        "localCoder.modelProfile": {
          "type": "string",
          "default": "qwen3-coder-30b-a3b-q4xl",
          "enum": [
            "qwen3-coder-30b-a3b-q4xl",
            "qwen3-coder-30b-a3b-iq2m",
            "muse-glimmer-30b-kquant",
            "muse-glimmer-30b-kquant-dynamic"
          ],
          "enumDescriptions": [
            "Recommended. 3.3B-active mixture-of-experts, no reasoning phase, has fill-in-the-middle.",
            "Low-memory fallback. Smaller but not proportionally faster on CPU, and weaker on code.",
            "Deep reasoning, dense 30B. Much slower on CPU; no fill-in-the-middle.",
            "Deep reasoning at higher precision. Slower again, and the largest footprint here."
          ],
          "description": "Model profile selected from the checked-in approved manifest."
        },
        "localCoder.modelDirectory": {
          "type": "string",
          "default": "",
          "description": "Directory for GGUF files. Empty uses a user-level LocalCoder models directory."
        },
        "localCoder.modelMirrorBaseUrl": {
          "type": "string",
          "default": "",
          "description": "Approved internal HTTPS base URL. The model file name is appended before public sources are tried."
        },
        "localCoder.network.allowPublicModelDownload": {
          "type": "boolean",
          "default": true,
          "description": "Allow direct download from non-Hugging-Face public sources listed in the manifest."
        },
        "localCoder.runtimePath": {
          "type": "string",
          "default": "",
          "description": "Optional path to a compatible llama-server binary or its directory. Empty uses the binary bundled in the platform VSIX."
        },
        "localCoder.runtime.contextSize": {
          "type": "integer",
          "default": 0,
          "minimum": 0,
          "maximum": 262144,
          "description": "Override context size. Zero uses the conservative model-profile default."
        },
        "localCoder.runtime.threads": {
          "type": "integer",
          "default": 0,
          "minimum": 0,
          "maximum": 256,
          "description": "CPU generation threads. Zero chooses a conservative automatic value."
        },
        "localCoder.runtime.promptCacheMiB": {
          "type": "integer",
          "default": 512,
          "minimum": 0,
          "maximum": 4096,
          "description": "Bounded llama-server prompt-cache RAM in MiB. Set to zero to disable; 512 is conservative for a 32 GB laptop."
        },
        "localCoder.runtime.port": {
          "type": "integer",
          "default": 0,
          "minimum": 0,
          "maximum": 65535,
          "description": "Loopback port. Zero selects a free ephemeral port."
        },
        "localCoder.runtime.autoStart": {
          "type": "boolean",
          "default": false,
          "description": "Start the runtime after extension activation when the selected model is already present."
        },
        "localCoder.runtime.startupTimeoutSeconds": {
          "type": "integer",
          "default": 300,
          "minimum": 30,
          "maximum": 1800,
          "description": "Maximum time to wait for a large model to become ready."
        },
        "localCoder.runtime.gpuLayers": {
          "type": [
            "string",
            "integer"
          ],
          "default": "auto",
          "minimum": 0,
          "maximum": 999,
          "description": "Layers to offload to the GPU. \"auto\" offloads as many as the device holds, \"off\" keeps generation entirely on the CPU, and a number pins the count. A machine with no usable GPU falls back to the CPU on its own."
        },
        "localCoder.runtime.enableDraftModel": {
          "type": "boolean",
          "default": false,
          "description": "Use the profile's draft model for speculative decoding when it is installed. Off by default: the drafter published for Muse Glimmer is a different architecture (dflash) from the model it drafts for, and llama.cpp refuses to create the context it needs, which fails the whole launch. If a run that offers a drafter fails, the runtime retries without it automatically."
        },
        "localCoder.runtime.draftMaxTokens": {
          "type": "integer",
          "default": 15,
          "minimum": 1,
          "maximum": 64,
          "description": "Maximum tokens the draft model proposes per step. Higher values help when the drafter agrees often and cost throughput when it does not. The effective ceiling comes from the drafter itself: a DFlash drafter spends one of its 16 block slots on the anchor token, so 15 is its maximum and larger values are clamped."
        },
        "localCoder.runtime.extraArguments": {
          "type": "array",
          "default": [],
          "items": {
            "type": "string"
          },
          "description": "Optional CPU scheduling/warm-up arguments from a strict allow-list; all other llama-server arguments are rejected."
        },
        "localCoder.inlineCompletions.enabled": {
          "type": "boolean",
          "default": false,
          "description": "Enable local fill-in-the-middle inline suggestions. The runtime must already be started."
        },
        "localCoder.inlineCompletions.maxTokens": {
          "type": "integer",
          "default": 96,
          "minimum": 16,
          "maximum": 512,
          "description": "Maximum tokens for an inline completion."
        },
        "localCoder.inlineCompletions.debounceMs": {
          "type": "integer",
          "default": 450,
          "minimum": 100,
          "maximum": 3000,
          "description": "Pause before an automatic inline request."
        },
        "localCoder.context.maxCharacters": {
          "type": "integer",
          "default": 16000,
          "minimum": 4000,
          "maximum": 250000,
          "description": "Maximum characters of workspace context attached to a request. Measured on the default profile, prompt processing is the dominant cost and it degrades with depth: 60 tok/s at 579 tokens, 37 at 2327, and 16 at 9227, where a single turn took 9.5 minutes on a 28-core machine. 48000 characters is roughly 12000 tokens and put every turn in that range, so the default is now 16000. Raise it only if you are willing to wait; the cost is paid on every turn."
        },
        "localCoder.context.maxRetrievedFiles": {
          "type": "integer",
          "default": 5,
          "minimum": 0,
          "maximum": 20,
          "description": "Maximum lexically selected workspace files added to a chat request."
        },
        "localCoder.context.extraFiles": {
          "type": "array",
          "default": [],
          "items": {
            "type": "string"
          },
          "description": "Workspace-relative glob patterns always considered for chat context, subject to secret/path exclusions."
        },
        "localCoder.context.useProjectMemory": {
          "type": "boolean",
          "default": true,
          "description": "Include .localcoder/memory.md from the workspace in every request. The file records standing project facts such as build and test commands. It is treated as untrusted workspace data, not as instructions, and is capped at 8,000 characters."
        },
        "localCoder.context.includeDiagnostics": {
          "type": "boolean",
          "default": true,
          "description": "Include active-document diagnostics in local prompts."
        },
        "localCoder.agent.mode": {
          "type": "string",
          "default": "off",
          "enum": [
            "off",
            "readonly",
            "allowlist",
            "confirm"
          ],
          "enumDescriptions": [
            "Agent tools are disabled entirely.",
            "The model may read, list, and search workspace files. No commands run.",
            "As readonly, plus commands whose argv matches localCoder.agent.allowedCommands.",
            "As readonly, plus any command, but each one must be confirmed."
          ],
          "description": "Whether the model may use workspace tools. Off by default. Secret and binary paths are excluded from reading in every mode, and commands never run through a shell."
        },
        "localCoder.agent.allowedCommands": {
          "type": "array",
          "default": [
            "git status",
            "git diff",
            "git log",
            "npm test",
            "npm run lint",
            "node --test"
          ],
          "items": {
            "type": "string"
          },
          "description": "Commands the agent may run in allowlist mode. Each entry is matched as an argv prefix, never as text, so \"npm test\" cannot authorise \"npm test; rm -rf ~\". This list replaces the built-in defaults rather than extending them."
        },
        "localCoder.agent.maxSteps": {
          "type": "integer",
          "default": 8,
          "minimum": 1,
          "maximum": 30,
          "description": "Maximum tool calls in one agent turn. Every step costs a full prompt evaluation, so a low cap keeps a confused model from spending minutes."
        },
        "localCoder.chat.persistHistory": {
          "type": "boolean",
          "default": false,
          "description": "Store this workspace's chat transcript so it survives restarting VS Code. Off by default: a transcript on disk outlives the session and can contain quoted source. It is written to the extension's private storage directory, never into the workspace, and \u201cClear conversation\u201d deletes it."
        },
        "localCoder.chat.maxHistoryTurns": {
          "type": "integer",
          "default": 6,
          "minimum": 0,
          "maximum": 20,
          "description": "Recent user/assistant turns retained in extension memory."
        },
        "localCoder.chat.reasoningStrength": {
          "type": "string",
          "default": "medium",
          "enum": [
            "off",
            "low",
            "medium",
            "high",
            "xhigh"
          ],
          "enumDescriptions": [
            "Send nothing, and let the model use its own default. For Muse Glimmer that default is 'high', the slowest setting.",
            "Least thinking, fastest first word. Measured at 73 analysis tokens against 589 at high.",
            "Balanced. The default.",
            "The model's own default: most thinking, slowest.",
            "Maximum thinking. Measured no better than high on a coding prompt, and no faster."
          ],
          "description": "How hard a reasoning model should think before answering. Thinking is generated at the same speed as the answer, so on a CPU this is the largest single influence on how long you wait: measured on one coding question, 'low' reached the first word of the answer in 27 seconds where 'high' took 147. Ignored by models with no reasoning channel. The short selection commands always ask for 'low', since you are waiting on them."
        },
        "localCoder.chat.maxOutputTokens": {
          "type": "integer",
          "default": 0,
          "minimum": 0,
          "maximum": 16384,
          "description": "Maximum tokens the model may generate for one chat reply. 0 uses the model profile's own limit. Raise this on a reasoning model: its private analysis is spent from the same budget, so a limit that is too low can be exhausted before the answer begins and the reply arrives empty. Every token added here is taken from the conversation history that fits alongside it."
        }
      }
    }
  },
  "scripts": {
    "test": "node --test test/*.test.js",
    "package": "npx --yes @vscode/vsce@3.9.2 package --no-dependencies"
  }
}
